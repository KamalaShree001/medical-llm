services:
  - type: web
    name: medical-llm
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
    packages:
      - tesseract-ocr
